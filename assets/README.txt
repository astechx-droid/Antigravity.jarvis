Pinterest often blocks downloads on pins like:
https://pin.it/4yGyHyHt4

You do NOT need a video file — Jarvis uses a built-in live animation.

Optional: if you ever get an MP4 from another source, name it
jarvis-wallpaper.mp4 and we can wire it back in.

To capture from Pinterest (last resort):
- Phone: screen record while the pin plays, trim, save as MP4
- PC: Win+G Game Bar → Record, play pin in browser, save clip
